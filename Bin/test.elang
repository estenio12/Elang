var:text t10 = "hello world";

fun:number mul(var:number value, var:number exp)
	return value * exp;
end

fun:number sum(var:number value, var:number exp)
	return value + exp;
end

fun:void print(var:number txt)

end

#sum(10, 10);


#t10 = mul(10, 10) + 50;

#var:number sg = mul+sum(2, 2);
var:number sg = print();

#op 0
#comma 0